;; Send mail using SMTP via smtp.nec.cn.
;;(setq smtpmail-smtp-server "smtp.nec.cn")
;; Send mail using SMTP on the mail submission port 25.
;;(setq smtpmail-smtp-service 25)

;;(custom-set-variables
;; '(send-mail-function '(smtpmail-send-it))
;; '(smtpmail-smtp-server "smtp.nec.cn" t)
;; '(smtpmail-smtp-service 25 t))


